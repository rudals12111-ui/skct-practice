# 검증 기록

검증일: 2026-09-25. 사용자 문제집은 제공되지 않았으므로 자체 제작한 텍스트 PDF(2쪽)와 스캔형 PDF(1쪽)를 사용했습니다.

## 자동 검사

`npm test` 8개 통과 (v1.0 기준):

- 비활성화된 탭이 여러 영역/안내 시간을 지나 복귀해도 종료 시각을 유지
- 안내 시간 0초, 문항별 시간 중복 집계 방지
- 일시정지 시간 제외 및 저장된 세션의 남은 시간 복원
- 정답 입력 문항만 채점, 미채점과 미응답 구분
- 계산기 연속 계산·소수·반복 등호·부호·0으로 나누기
- 2단 문항 번호 후보와 읽기 순서, 쪽번호 제외
- 이미지 PDF의 텍스트 후보 없음 및 좌표 정규화
- JSON 문제집의 외부 이미지/잘못된 영역 거부, 중복 ID 처리, CSV 수식 문자 처리

작성한 JavaScript 파일의 구문 검사 통과. 실행기에서 별도 포트의 서버 시작 → HTTP 응답 확인 → 해당 서버만 종료하는 검사 통과.

## 실제 UI에서 확인

- 화면의 답안 선택과 OMR 동기화
- 계산기 키보드 입력 `12 + 3 = 15`
- 메모 입력, 다음 문항 이동 시 메모·계산기 초기화
- 마우스 그림판 필기와 되돌리기
- 제출 확인창과 결과 채점, 정답 2/10 결과 확인
- 텍스트 PDF 렌더링 및 문항 번호 2개 감지·자르기
- 스캔 PDF의 텍스트 없음 안내, 상하 분할·문항 적용
- 드래그 직접 자르기(1245×504px 선택 영역)와 새 문항 추가
- 공통 지문 + 문항 = 2조각, 이어 붙이기 후 3조각, 되돌리기 후 2조각
- 실전 모드에서 이전 버튼 비활성화, 일시정지 숨김, OMR 기본 숨김
- 0.1분(6초) 시험 만료 자동 제출, 정답 없는 두 문항은 미채점
- 브라우저 새로고침 후 적용 문제집·편집 목록·원본 PDF 복원
- 일시정지 직후 새로고침 시 13:14 → 13:14, 계속하기 버튼 복원
- 일반 Chrome에서 결과 CSV 저장 완료. 다운로드 파일을 다시 읽어 답안·정답·결과 열 확인
- 넓은 데스크톱과 약 490px 폭의 앱 내 브라우저에서 배치 확인
- 읽기 전용 WebMCP 상태 도구의 등록과 정상 호출 확인

## 수정한 문제

일시정지 직후 새로고침하면 IndexedDB의 비동기 저장보다 페이지 종료가 먼저 일어날 수 있었습니다. 문제집은 IndexedDB에 저장하고, 작은 세션 상태는 localStorage에도 즉시 기록하도록 보완했습니다. 복원 시 저장 시각이 더 최근인 상태를 선택합니다.

## 검증 범위의 한계

- 임의 출판사의 모든 편집·문항 번호 형식에 대한 자동 분할 정확도는 보장하지 않습니다. 분할 후보 확인과 수동 보정이 필요합니다.
- 암호 PDF와 사용자의 실제 300MB 문제집은 검증하지 않았습니다. 아래 대용량 읽기 검사는 자체 제작 입력을 사용했습니다.
- JSON 복원의 전체 UI 왕복은 완료하지 않았습니다. JSON 검증 함수는 자동 검사했습니다.
- Chrome 자동 파일 선택은 확장 프로그램의 파일 URL 접근 권한 때문에 제한되어 PDF 검사는 앱 내 브라우저에서 수행했습니다. 브라우저의 일반 수동 파일 선택을 막는 앱 오류는 아닙니다.
- 앱 내 브라우저의 다운로드 완료 이벤트는 확인되지 않았습니다. 일반 Chrome에서는 동일한 저장 함수가 CSV 파일을 정상 저장했습니다. 최종 파일 백업은 일반 Chrome/Edge를 권장합니다.
- 공식 시험 프로그램의 원본 UI는 확보하지 못했습니다. 조사 근거와 재현 범위는 `dist/research.html`에 구분했습니다.

## 2026-09-25 · v1.1 대용량 PDF와 회차 범위

- 자동 검사 11개 통과: 기존 8개 및 파일 부분 읽기/중단, 범위·크기 오류, 실제 PDF.js 엔진 통합 검사.
- 자체 제작 300MiB PDF에서 2쪽 텍스트 추출 성공. 파일 전체 300MiB 중 2MiB를 두 번의 구간 읽기로 처리했습니다. 이 입력은 미참조 대용량 스트림을 포함하는 2쪽 합성 PDF이며 실제 300MB 스캔 문제집의 메모리·속도를 대표하지 않습니다. 테스트 파일은 검사 후 삭제됩니다.
- 별도 포트 8788 UI에서 2쪽짜리 텍스트 PDF → 2~2쪽 범위 열기 → 범위 밖 이전 쪽 이동 차단 → 상하 2문항 일괄 추가 확인. 목록 두 문항 모두 2쪽으로 표시됩니다.
- 추출 문항 정답 변경 및 원본 PDF 닫기 후 문항 2개 유지 확인. 브라우저 오류 로그 없음. 범위 지정 패널 시각 확인.
- 원본 한도 1GiB, 1MiB 단위 요청 읽기, 자동 선읽기 비활성화. 32MiB를 넘는 원본은 이름·크기만 저장하고 추출 문항은 별도로 저장합니다.
- PDF 구조/손상 복구에 따라 선택 영역 밖 데이터도 읽을 수 있으며 메모리 상한을 보장하지 않습니다.

## 2026-09-25 · v1.2 자동 문항 인식(OCR)과 실전 구성

- 자동 검사 22개 통과: 기존 13개(코어·대용량 PDF) + 실전 규격·영역당 20문항 제한·번호 보존 2개 + 자동 인식 7개(번호 토큰, 영역명 오자 허용, 번호 복구·경고, 색 상자 반전, 2단·공통 지문·꼬리말·측면 탭, 수열·선지 오인 방지, 교재 4쪽 실측).
- 교재 4쪽(사용자 캡처로 만든 이미지 전용 PDF, 텍스트 없음)으로 Node와 브라우저(Chromium) 양쪽에서 실제 OCR 실행:
  - 1쪽 13번, 2쪽 08번, 3쪽 01번(영역 ‘언어이해’ 자동 인식, 제목 상자 제외), 4쪽 04·05·06·07번.
  - 4쪽의 04·06번은 OCR 전체 인식에서 빠졌으나 번호 열 잉크 분석으로 복구.
  - 모든 문항에서 쪽번호·출판사 꼬리말·오른쪽 색인 탭 제외, ‘기출 키워드’ 꼬리표 가림 확인(겹침 이미지 육안 확인).
  - 브라우저 처리 시간: 쪽당 약 7초, 4쪽 범위 추출 약 29초. 콘솔 오류 없음.
- 기존 자체 제작 텍스트 PDF(텍스트 레이어 경로)와 스캔 PDF(OCR 경로)에서 각 01·02번만 인식. 수열 줄 ‘2, 4, 8, 16’과 선지 ‘① 20’을 번호로 오인하던 문제를 발견해 수정.
- 후보 제외(✕)·후보별 영역 변경 → 추가, 실전 규격 적용, 문항 번호 바로 앞 문항 건너뛰기·이전 문항 잠금, 지우개·되돌리기 숨김, 영역별 결과 표를 브라우저에서 확인.
- 수정한 결함: 기존 `ocr.js`는 번들된 tesseract ESM이 기본 내보내기만 제공해 `createWorker is not a function`으로 실행되지 않았음.

### 한계

- 검증한 교재 편집은 1종(1단, 번호 왼쪽 정렬)입니다. 2단·공통 지문은 합성 페이지로만 검증했습니다.
- 흐린 스캔, 기울어진 촬영본, 번호가 발문과 같은 줄 안쪽에 들여 쓰인 편집에서는 누락·오인이 생길 수 있습니다. 쪽별 경고와 미리 보기로 검수하세요.
- 쪽을 넘는 문항 자동 이어 붙이기는 ‘첫 문항 위에 머리말이 아닌 본문이 있다’는 규칙에 따르며 실제 교재 사례로는 검증하지 못했습니다.

## 2026-09-25 · v1.2.1 문항 하나뿐인 쪽 보강

- 증상: 사용자 PC에서 2026 렛유인 교재 29쪽(25하 모의고사 언어이해 01번, 제목 상자와 같은 쪽)이 인식되지 않음. PDF에는 텍스트 층이 없음(확인).
- 원인: 번호 열 위치를 OCR이 읽은 번호로 잡는 구조라, 문항이 하나뿐인 쪽에서 OCR이 `01.`을 놓치거나 `이.`처럼 잘못 읽으면 기준점이 없어 0문항이 됨. 검증 환경에서는 읽혔으나 OCR 결과는 실행 환경에 따라 달라질 수 있음.
- 수정: 번호를 하나도 못 찾으면 "~것은?" 같은 발문 줄을 기준으로 그 왼쪽의 번호 덩어리를 찾아 숫자 전용 인식으로 다시 읽음. 이 경로를 탄 쪽에는 확인 경고를 표시.
- 검증: 29쪽 OCR 결과에서 번호를 지운 경우·`이.`로 바꾼 경우 모두 01번을 같은 위치로 복구(자동 검사 추가, 총 23개 통과).
- 실제 교재 29~60쪽(25하 모의고사 언어이해·자료해석) 전체 실행: 언어이해 1~20번, 자료해석 1~20번 누락 없이 인식, 두 영역 모두 첫 쪽 제목 상자에서 영역 자동 전환. 표·〈보기〉 포함 문항의 잘림 영역 육안 확인. 쪽당 약 2~6초.

## 2026-09-26 · v1.3 정답표 인식과 자동 채점

- 입력: 사용자가 보낸 2025 하반기 모의고사 정답표 캡처 5장(영역당 20문항, 약 380×180px의 작은 이미지).
- 초기 시도: 쪽 단위 OCR은 번호 줄(연보라 배경)을 통째로 놓치고 동그라미 숫자를 `@`, `®`, `0)`로 읽어 사용 불가. 동그라미 안 숫자만 OCR해도 100칸 중 56칸.
- 채택 방식: 음영 번호 줄·칸 구분선으로 표 구조를 픽셀 분석 → 칸마다 가장자리에 닿지 않는 덩어리만 글자로 추출 → 원 테두리 제거 후 안쪽 숫자를 서체 견본과 비교(99/100) → 같은 책 숫자로 견본을 다시 만들어 재판별(100/100).
- 결과: 5장 모두 영역명 5개, 문항 번호 1~20, 정답 100칸 일치. 한 장만 넣어도 동일. 브라우저(Chromium)에서 이미지 선택 → 인식 → 적용 → 재채점 흐름 확인, 콘솔 오류 없음.
- 발견한 사람 오류: 앞서 수작업으로 옮긴 창의수리 07번 정답을 ③으로 잘못 적었음. 원본 확대 결과 ⑤이며 판별기가 맞았음. 이전 채점 72점 → 73점으로 정정.
- 오인 방지: 교재 PDF 668쪽 전체를 훑었을 때 자료해석 문제 속 자료 표 8곳이 정답표 후보로 잡혔으나, 정답 칸에 크기가 고른 정사각형 글자가 있어야 한다는 조건으로 모두 제외. 이 PDF에는 정답표 쪽이 없음(해설은 별도 책).
- 문항 매칭: 인식된 번호가 1~N으로 중복 없이 맞으면 번호 기준, 아니면 출제 순서 기준. 사용자 결과의 언어추리 13·14번 자리(번호 8·12로 잘못 붙음)는 순서 기준으로 처리됨을 검사.
- 자동 검사 28개 통과(정답표 5개 추가).

### 한계

- 검증한 정답표 편집은 1종(음영 번호 줄 + 동그라미 정답)입니다. 번호와 정답이 한 칸에 함께 있는 표(예: 1-③)나 가로로 긴 1행 표는 인식하지 못할 수 있습니다. 이때는 숫자 20자리를 직접 입력하세요.
- 캡처가 흐리거나 너무 작으면(칸 높이 10px 미만) 판별이 불안정해집니다. 노란 칸을 확인하세요.

## 2026-09-26 · v1.4 문제·정답 통합 인식

- 흐름: ① 문제 쪽 범위 → ② 정답 쪽 범위(같은 PDF 또는 별도 정답 PDF) → ③ 통합 인식. 추출한 문항에만 정답을 적용하고, 결과·불확실 칸을 기록에 남김.
- 검증 입력: 문제는 사용자 교재 4쪽(book-sample.pdf), 정답은 정답표 캡처 5장을 한 쪽에 배치한 2쪽짜리 합성 PDF(쪽마다 1회차, 총 2회차). 실제 해설 PDF는 확보하지 못함.
- 브라우저(Chromium): 문항 7개 추출 → 정답 PDF 2쪽 인식 → 회차 묶음 2개 구분 → 1번째 적용 시 13·8·1·4·5·6·7번에 정답 4·2·1·5·3·4·3 부착(원본과 일치) → ‘정답만 인식해 적용’으로 2번째 묶음 재적용 → 확인·수정 창 미리 채움 확인. 콘솔 오류 없음.
- 수정한 문제 1 (PDF.js 5.4.624 버그): 쪽 수를 문서별이 아닌 전역(PagesMapper 정적 변수)에 저장해, 두 번째 PDF(정답 2쪽)를 열면 문제 PDF 3쪽부터 "Invalid page request"로 거부됨. Node에서 일반 PDF 두 개로도 재현. 쪽을 요청하기 직전에 해당 문서의 쪽 수로 맞추는 방식으로 우회.
- 수정한 문제 2: 해설 쪽처럼 표가 쪽 폭의 절반뿐이면 번호 줄을 놓침. 쪽 폭 비율 대신 음영이 이어지는 길이로 찾고, 한 줄 안에서 음영이 끊기면 표를 나눔(나란히 놓인 표 대응). 영역명 탭과 번호 줄이 한 띠로 붙는 경우도 분리.
- 수정한 문제 3: 둘째 줄 번호 11~20을 OCR이 1~10으로 읽는 경우, 같은 표에서는 번호가 앞 줄보다 커야 한다는 조건으로 보정.
- 수정한 문제 4: 두 번 축소·확대된 흐린 정답표에서 ④를 ①로 판별. 글자마다 명암 기준을 자동으로 정하는 방식(Otsu)으로 바꿔 정답 PDF 200칸·스크린샷 100칸 모두 일치.
- 자동 검사 29개 통과.

## 2026-09-26 · v1.4.1 실제 해설 쪽 레이아웃 대응

- 입력: 사용자가 보낸 실제 해설 2쪽(수열추리·언어추리 첫 쪽). 정답표(영역명 탭 + 2줄) 옆에 영역 제목·해설 본문·꼬리말·측면 탭이 있고, 언어추리 쪽에는 음영 머리줄이 있는 다른 표(참/거짓 T/F 표, 자리 배치 표)가 오른쪽 단에 나란히 있음.
- 발견: 오른쪽 단 참/거짓 표의 머리줄(대각선 칸)이 세로로 길어, 왼쪽 정답표의 첫 줄 답과 둘째 줄 번호가 한 띠로 묶여 둘째 줄(11~20)을 놓침.
- 수정: 띠를 표별 구간으로 나눈 뒤 구간마다 행 높이를 다시 계산. 정답 칸 글자에 원형 테두리가 있어야 정답표로 인정(48방향 원주 검사, 70% 이상). T/F·A/B 글자 표는 제외.
- 결과: 두 쪽 모두 영역명·번호 1~20·정답 20칸 일치, 다른 표는 모두 제외. 정답표 쪽과 표 없는 쪽이 섞인 4쪽 해설 PDF로 브라우저 통합 흐름 확인(1·3쪽에서만 정답표 발견, 40칸 일치).
- 메모리: 해설 전체를 정답 범위로 잡아도 되도록, 쪽마다 정답표 영역만 잘라 두고 쪽 이미지는 즉시 해제.
- 자동 검사 30개 통과.

## 2026-09-26 · v1.5 채점 후 복기 화면

- 결과의 ‘보기’ 또는 제출 후 문항 이동 시: 답안 칸에 내 답(오답이면 빨강)과 정답(초록 테두리)을 함께 표시하고, 오른쪽에 ‘내 답 · 정답 · 정답/오답/미응답(오답)/미채점’ 표시.
- 문항 번호 바와 OMR에 결과 색(초록 정답, 빨강 오답, 회색 미응답), 영역 진행 표시는 영역별 정답 수로 바뀌고 눌러서 영역 이동.
- 복기 중에는 이전·다음 버튼이 영역 경계를 넘어 이어짐(마지막 문항 다음은 결과 창).
- 브라우저 확인: 예제에서 1번 정답(③)·2번 오답(⑤, 정답 ②) 표시, 다음 영역 첫 문항의 미응답(오답) 표시, 이전 버튼으로 앞 영역 복귀, 영역 표시 클릭 이동. 콘솔 오류 없음.

## 2026-09-26 · v1.6 결과 필터

- 결과 창: 영역 탭과 결과 필터(개수 표시), 영역 요약 표의 영역 이름 클릭 필터, 조건에 맞는 문항이 없을 때 안내.
- 걸러진 목록에서 복기를 시작하면 이전·다음이 그 목록 안에서만 이동하고, 안내 문구에 ‘걸러진 문항 복기 · n / 전체 (조건)’ 표시. 목록 밖 문항으로 번호 바를 눌러 이동하면 일반 복기로 전환.
- 브라우저 확인(예제 10문항, 1정답·1오답·8미응답): 전체 10 → 오답+미응답 9 → 언어이해+오답+미응답 1, 필터 개수 표시, 미응답 필터 복기에서 1/8 → 2/8 → 1/8 이동. 콘솔 오류 없음.

## 2026-09-26 · v1.7 오프라인 설치(PWA)와 GitHub Pages 묶음

- 추가: manifest·아이콘·서비스 워커(빌드 시 파일 목록과 버전 해시 포함, 새 버전 설치 시 이전 캐시 삭제), 터치 기기용 크기 조정, 안전 영역 여백.
- 빌드: `tools/build_pages.py`가 dist만 복사(_diag 제외) → 227개 파일, 압축 11.5MB를 9MB 이하 2조각으로 분할. GitHub 웹 업로드는 폴더 구조를 유지하지 못해, 조각을 합쳐 푸는 Actions 배포 방식을 택함.
- 검증(Chromium): 첫 방문에 228개 파일 캐시 → 네트워크 차단 후 새로고침 시 앱 표시, OCR·PDF 모듈과 한국어 언어 데이터 캐시에서 로드, 오프라인 OCR로 문항 2개 인식.
- 미검증: 아이패드 Safari 실기기(설치·저장 한도·터치 조작).

## 2026-09-26 · v1.7.1 영역 표기(흰 글씨 탭) 인식 보강

- 증상: 2회차 132쪽(수열추리 1번)이 언어추리 21번째 문항처럼 이어서 들어감. 탭 글자를 딱 맞게 자른 영역으로 OCR해 ‘스열츠긴’으로 읽혀 영역을 못 찾음.
- 수정: 채워진 탭은 흰 여백을 두고 2배 확대한 조각으로 따로 OCR한 뒤 좌표를 되돌림(브라우저·Node 공통). 영역 표기를 끝내 못 읽었는데 번호가 1번부터 다시 시작하면 다음 영역으로 넘기고 경고 표시.
- 결과: 29~137쪽 두 회차 10개 영역 시작 쪽이 모두 맞는 영역으로 인식(132쪽 수열추리 포함). 자동 검사 31개 통과.

## 2026-09-26 · v1.8 GitHub 저장소를 원본으로

- 저장소 구조를 사이트 압축 조각에서 실제 소스 파일로 전환. 배포 워크플로가 업로드된 src.zip 조각을 한 번 풀어 커밋한 뒤, `build_pages.py --site`로 sw.js를 만들어 Pages에 게시.
- GITHUB_TOKEN은 워크플로 파일을 수정할 수 없어 `.github/workflows/pages.yml`은 번들에 넣지 않고 웹 편집기로 관리.

## 2026-09-26 · v1.9 수동 보정 후 자동 재배치

- 정렬 규칙(core.js orderQuestions): 회차(같은 영역·번호의 반복 횟수) → 영역 순서 → 번호 → 기존 위치. 번호 없는 문항은 같은 영역에서 바로 앞 번호 뒤에 고정.
- 자동 검사: 잘못 들어간 수열추리 1~3 수정, 번호 없는 문항 위치, 두 회차 비섞임, 이름 칸 해석(‘수열추리 7’, ‘12번’, ‘언어 이해 3번 (지문)’). 전체 32개 통과.
- 브라우저 확인: 번호 칸으로 수동 추가(3 → 1 입력 시 1, 3 순서, 다음 번호 자동 증가), 언어추리 20번을 수열추리 2번으로 변경 시 수열추리 1·2·3 순서로 재배치, 이름 칸 ‘수열추리 4’ 입력 시 번호·영역 반영, 언어추리 1~3을 수열추리로 바꿀 때 뒤 2문항 함께 이동 확인 후 언어추리 19·20 → 수열추리 1·2·3 순서. 콘솔 오류 없음.
